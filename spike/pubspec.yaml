name: torph_render_spike
description: "RENDER-SPIKE-001: experimental evidence only. Not production."
version: 0.0.0
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  torph:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
